<?xml version="1.0" encoding="utf-8"?>
<!-- Matches SplashScreen's dark background + logo so there is no
     black flash between the native launch window and the first
     Flutter frame. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/melora_logo" />
    </item>
</layer-list>